function showBtnHidden(index) {
  showBtn[index].style.visibility = "hidden";
}

function hideBtnVisible(index) {
  hideBtn[index].style.visibility = "visible";
}

function hideBtnHidden(index) {
  hideBtn[index].style.visibility = "hidden";
}

function showBtnVisible(index) {
  showBtn[index].style.visibility = "visible";
}

function hrVisible(index) {
  hr[index].style.visibility = "visible";
}

function hrHidden(index) {
  hr[index].style.visibility = "hidden";
}

function contentVisible(index) {
  content[index].style.display = "block";
}

function contentHidden(index) {
  content[index].style.display = "none";
}
